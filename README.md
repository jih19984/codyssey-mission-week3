# Mini NPU Simulator

## 프로젝트 개요

<!-- 작성 필요: AI의 MAC(Multiply-Accumulate) 연산을 흉내 내는 콘솔 프로그램이라는 점,
     왜 이 미션을 하는지(NPU/MAC 연산 원리 학습) 1~2문장으로 소개 -->

## 실행 방법

```bash
python3 main.py
```

- 실행하면 `1) 사용자 입력(3x3)` / `2) data.json 분석` 중 모드를 선택한다.
- `data.json`은 `main.py`와 같은 위치(프로젝트 루트)에 있어야 한다.
- Python 3.8 이상, 외부 라이브러리 없이 표준 라이브러리(`json`, `time`)만 사용한다.

## 파일 구조

```
codyssey-mission-week3/
├── main.py                 # 진입점 (NpuSimulatorApp 생성 후 run() 호출)
├── app.py                  # 전체 실행 흐름 오케스트레이션 (모드1/모드2)
├── matrix.py                # Matrix 클래스: n×n 데이터 구조(get/set), 콘솔 입력 검증
├── mac_engine.py             # MAC 연산 + epsilon 기반 점수 비교/판정
├── data_loader.py            # data.json 로드, 스키마 검증, 라벨 정규화
├── performance.py            # 반복 측정 및 성능 표 출력
├── report.py                  # PASS/FAIL 집계 및 결과 요약
├── pattern_generator.py       # [보너스] N×N 십자가/X 패턴 자동 생성
├── data.json                  # 필터(5x5/13x13/25x25) 및 패턴 샘플 데이터
├── docs/
│   ├── concepts/                 # 진행하며 정리한 개념 노트
│   │   └── floating_point.md      # 부동소수점 오차 / epsilon 비교 정책
│   └── TROUBLESHOOTING.md          # 개발 중 겪은 이슈 및 해결 기록
└── README.md
```

## 구현 요약

<!-- 작성 필요 -->
- 라벨 정규화 방식: (LABEL_MAP으로 '+'/'cross'/'x' 등을 표준 라벨 Cross/X로 매핑하는 방식 설명)
- MAC 연산 구현 개요: (반복문으로 위치별 곱셈 후 누적합하는 로직 설명, 외부 라이브러리 미사용 명시)
- 동점 처리 정책(epsilon): (EPSILON=1e-9 기준과 그 이유 설명)

## 결과 리포트

> data.json 분석 모드를 실행한 뒤, 아래 항목을 실제 결과로 채워 넣는다. (최소 10줄 이상 서술)

### FAIL 케이스 원인 분석

<!-- 작성 필요. data.json에는 의도적으로 아래 두 종류의 FAIL 케이스가 포함되어 있다.
     각각 실제 실행 결과를 바탕으로 원인을 서술할 것.
     - size_13_3: 13x13 전부 1로 채운 입력 -> cross/x 필터 모두 1의 개수가 2*13-1=25로 동일해
       정확히 동점(25.0 vs 25.0) -> UNDECIDED -> expected와 달라 FAIL (비교 정책 이슈)
     - size_25_3: 키는 size_25_3이지만 실제 input이 24x24로 저장되어 있음 -> 필터(25x25)와
       크기가 맞지 않는 스키마 문제 -> 프로그램이 죽지 않고 케이스 단위로 FAIL 처리되어야 함
     실패가 0개로 나왔다면, 정규화/epsilon 처리를 어떻게 했길래 0개가 되었는지도 설명 -->

### 성능 표 해석 및 시간 복잡도(O(N²))

<!-- 작성 필요: data.json 모드 실행 후 나온 성능 표를 아래에 붙여넣고 해석 -->

```
크기       평균 시간(ms)    연산 횟수
-------------------------------------
3×3
5×5
13×13
25×25
```

<!-- 연산 횟수(N²)와 측정된 평균 시간의 관계를 근거로 O(N²)를 설명 -->

## 재현성 체크리스트

- [ ] 모드1: 3x3 십자가/X 필터 예시 입력 시 점수/판정/연산 시간이 정상 출력된다.
- [ ] 모드2: `data.json` 분석 시 케이스별 PASS/FAIL과 총합(총/통과/실패)이 일치한다.
- [ ] 의도적 오류 입력(행/열 개수 불일치, 숫자 아님)에 안내 문구 출력 후 재입력이 유도된다.
- [ ] `data.json`의 `size_25_3`(크기 불일치) 케이스에서 프로그램이 중단되지 않고 FAIL로 처리된다.
